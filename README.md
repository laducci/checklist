entra no projeto
abre o terminal

npm install
(se der ruim npm audit fix --force)

cd backend
npm run dev


abre mais um terminal
cd frontend 
npm run dev

copiar o .env.example
criar .env
colar o .env.example